## K08, Salt Lamp Horse
### DISCOVERIES
* python's "replace" replaces all instances of a string by default
* the zip command can combine two lists like a zipper!
* Some drivers are allergic to documentation.

### QUESTIONS, COMMENTS, CONCERNS
* Is data normally stored more like the first or second file?
* Every time the token used for commas or new line changes, we'll need to change the code to accomodate. 

### NOTES FROM THE FIELD
* You can use str.casefold to ignore case when sorting a string
* You can use .pop with lists to remove values by index
* If you're writing a long if statement that goes off the screen, there's probably a better way to do it.
