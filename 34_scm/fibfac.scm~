#lang racket

;Natalie Keiger

(define fact
  (lambda (n)
    (if (= n 0)
      1
      (* n (fact (- n 1)))))

(define fib
  lambda (n)
    (if (= n 0)
      1
      (+ n (fib (- n 1)))))

(display ())
